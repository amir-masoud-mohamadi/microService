server:
 port: 8080

spring:
 application:
  name: eureka-server

eureka:
 instance:
  hostname: localhost
 client:
  registerWithEureka: false # خودش رو ثبت نکنه!
  fetchRegistry: false # رجیستری رو نگه نداره
  serviceUrl:
   defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/